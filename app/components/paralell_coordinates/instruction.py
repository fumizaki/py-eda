def instruct_paralellcoor() -> str:
    lang = {
        'JA': {
            'instruction': """
        **目的:**
        - 単一の量的変数の分布（データの形状）を視覚化します。データをいくつかの区間（ビン、bin）に分割し、各区間に含まれるデータポイントの数を棒グラフで表示します。
                 
        **示唆:**
        - **分布の形状:** データが山形（正規分布に近い）、一様、右に裾が長い（右に歪んでいる）、左に裾が長い（左に歪んでいる）、二峰性（ピークが2つある）など、どのような形状をしているかを確認できます。
        - **中心傾向:** データがどの値を中心に分布しているか（最も頻度が高い区間はどこか）を把握できます。
        - **ばらつき:** データが狭い範囲に集中しているか、広い範囲に散らばっているかを確認できます。
        - **外れ値の可能性:** 他の区間から離れた場所に少数のデータが存在する場合、外れ値の可能性を示唆します。
        """
        }
    }

    return lang['JA']['instruction']
